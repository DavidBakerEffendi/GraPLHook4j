CREATE VERTEX TRAIT (PRIMARY_ID id UINT, name STRING)
CREATE VERTEX ARRAY_INITIALIZER (PRIMARY_ID id UINT, astOrder INT)
CREATE VERTEX BINDING (PRIMARY_ID id UINT, name STRING, signature STRING)
CREATE VERTEX BLOCK (PRIMARY_ID id UINT, name STRING, astOrder INT, argumentIndex INT, typeFullName STRING, lineNumber INT)
CREATE VERTEX CALL (PRIMARY_ID id UINT, name STRING, signature STRING, code STRING, methodInstFullName STRING, methodFullName STRING, argumentIndex INT, dispatchType STRING, typeFullName STRING, lineNumber INT)
CREATE VERTEX CONTROL_STRUCTURE (PRIMARY_ID id UINT, name STRING, signature STRING, code STRING, methodInstFullName STRING, methodFullName STRING, argumentIndex INT, dispatchType STRING, typeFullName STRING, lineNumber INT)
CREATE VERTEX CALL (PRIMARY_ID id UINT, name STRING, argumentIndex INT, astOrder STRING, lineNumber INT)
CREATE VERTEX FIELD_IDENTIFIER (PRIMARY_ID id UINT, canonicalName STRING, code STRING, argumentIndex INT, astOrder INT, lineNumber INT)
CREATE VERTEX FILE (PRIMARY_ID id UINT, name STRING, astOrder INT)
CREATE VERTEX IDENTIFIER (PRIMARY_ID id UINT, name STRING, code STRING, argumentIndex INT, astOrder INT, typeFullName STRING, lineNumber INT)
CREATE VERTEX LITERAL (PRIMARY_ID id UINT, name STRING, argumentIndex INT, astOrder INT, typeFullName STRING, lineNumber INT)
CREATE VERTEX LOCAL (PRIMARY_ID id UINT, name STRING, code INT, astOrder INT, typeFullName STRING, lineNumber INT)
CREATE VERTEX MEMBER (PRIMARY_ID id UINT, name STRING, code STRING, astOrder INT, typeFullName STRING)
CREATE VERTEX META_DATA (PRIMARY_ID id UINT, language STRING, version STRING)
CREATE VERTEX METHOD_PARAMETER_IN (PRIMARY_ID id UINT, name STRING, code STRING, evaluationStrategy STRING, astOrder INT, typeFullName STRING, lineNumber INT)
CREATE VERTEX METHOD_REF (PRIMARY_ID id UINT, code STRING, argumentIndex INT, astOrder INT, methodInstFullName STRING, methodFullName STRING, lineNumber INT)
CREATE VERTEX METHOD_RETURN (PRIMARY_ID id UINT, name STRING, typeFullName STRING, astOrder INT, evaluationStrategy STRING, lineNumber INT)
CREATE VERTEX METHOD (PRIMARY_ID id UINT, name STRING, fullName STRING, astOrder INT, signature STRING, lineNumber INT)
CREATE VERTEX MODIFIER (PRIMARY_ID id UINT, name STRING, astOrder INT)
CREATE VERTEX NAMESPACE_BLOCK (PRIMARY_ID id UINT, name STRING, astOrder INT, fullName STRING)